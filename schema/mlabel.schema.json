{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "$schema": {
      "$ref": "#/$defs/__schema0"
    },
    "version": {
      "$ref": "#/$defs/__schema1"
    },
    "ui": {
      "$ref": "#/$defs/__schema2"
    },
    "network": {
      "$ref": "#/$defs/__schema7"
    },
    "ai": {
      "$ref": "#/$defs/AiConfig"
    },
    "input": {
      "$ref": "#/$defs/__schema14"
    },
    "output": {
      "$ref": "#/$defs/__schema109"
    }
  },
  "required": ["version", "input", "output"],
  "additionalProperties": false,
  "$defs": {
    "__schema0": {
      "type": "string",
      "description": "Path or URL to this JSON Schema, for editor autocomplete. Use `https://mlabel.vlad.gg/mlabel.schema.json` for a standalone config."
    },
    "__schema1": {
      "type": "number",
      "const": 2,
      "description": "Config format version. This build reads version 2 only; a config with no `version`, or a different one, is rejected with a single clear message rather than a cascade of shape errors."
    },
    "__schema2": {
      "type": "object",
      "properties": {
        "appTitle": {
          "$ref": "#/$defs/__schema3"
        }
      },
      "additionalProperties": false,
      "description": "Chrome-level presentation."
    },
    "__schema3": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema4"
        },
        {
          "$ref": "#/$defs/__schema5"
        }
      ],
      "description": "The window title: a literal string, or `{ \"field\": \"…\" }` to use the value of an input field, which changes per record."
    },
    "__schema4": {
      "type": "string"
    },
    "__schema5": {
      "type": "object",
      "properties": {
        "field": {
          "description": "Input field whose value becomes the window title, so the title tracks the record on screen.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["field"],
      "additionalProperties": false
    },
    "__schema6": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_-][A-Za-z0-9_ -]*[A-Za-z0-9_-]$|^[A-Za-z0-9_-]$"
    },
    "__schema7": {
      "default": {
        "updateChecks": true,
        "modelDownload": true
      },
      "description": "Network policy. These are the only remote requests this app ever performs, and both are opt-out. With both `false` the app makes no network calls at all.",
      "$ref": "#/$defs/__schema8"
    },
    "__schema8": {
      "type": "object",
      "properties": {
        "updateChecks": {
          "default": true,
          "description": "Whether to check GitHub Releases for updates. Absent or `true` means checks run; `false` forbids that traffic entirely.",
          "$ref": "#/$defs/__schema9"
        },
        "modelDownload": {
          "default": true,
          "description": "Whether the app may download an anomaly-detection model from Hugging Face. Absent or `true` means it may, once a labeler asks for it in Settings — nothing is fetched otherwise. `false` forbids that traffic entirely, and hides the feature unless a model is already on this machine.",
          "$ref": "#/$defs/__schema10"
        }
      },
      "additionalProperties": false
    },
    "__schema9": {
      "type": "boolean"
    },
    "__schema10": {
      "type": "boolean"
    },
    "AiConfig": {
      "default": {
        "anomalyDetection": true
      },
      "title": "AI",
      "description": "On-device assistance. Suggestions are advisory: they are never written to the output file and can never fill in an answer.",
      "$ref": "#/$defs/__schema11"
    },
    "__schema11": {
      "type": "object",
      "properties": {
        "anomalyDetection": {
          "default": true,
          "description": "Whether labelers may enable on-device anomaly detection for this project. Absent or `true` offers it in Settings; nothing runs or downloads until it is switched on there. `false` removes the feature entirely — no section, no panel, no suggestions. Worth setting for a task where a model's guess sitting beside the answer could bias the labeler.",
          "$ref": "#/$defs/__schema12"
        },
        "context": {
          "description": "What this data is, in your own words, given to the model along with each row. Column names and types tell it what the data *is*; only you can tell it what the data *means* — what the file is, which columns relate to which, and what would count as odd here. Without this a small model is guessing at the shape of a row. Keep it to a short paragraph: it is sent with every record, and a long one crowds out the record itself. Never include instructions about what to conclude — describe the data, not the answer.",
          "$ref": "#/$defs/__schema13"
        }
      },
      "additionalProperties": false
    },
    "__schema12": {
      "type": "boolean"
    },
    "__schema13": {
      "type": "string",
      "maxLength": 2000
    },
    "__schema14": {
      "type": "object",
      "properties": {
        "adapterId": {
          "$ref": "#/$defs/__schema15"
        },
        "adapterConfig": {
          "$ref": "#/$defs/__schema17"
        },
        "fields": {
          "$ref": "#/$defs/__schema19"
        },
        "rules": {
          "$ref": "#/$defs/__schema68"
        },
        "cards": {
          "$ref": "#/$defs/__schema102"
        }
      },
      "required": ["fields"],
      "additionalProperties": false,
      "description": "What is read from the source file and shown read-only."
    },
    "__schema15": {
      "default": "csv",
      "description": "Which format adapter handles this side. `csv` is the only one built in; it also reads `.tsv`.",
      "$ref": "#/$defs/__schema16"
    },
    "__schema16": {
      "type": "string",
      "minLength": 1
    },
    "__schema17": {
      "type": "object",
      "properties": {},
      "additionalProperties": {
        "$ref": "#/$defs/__schema18"
      },
      "description": "Opaque, adapter-owned options — for CSV, `delimiter` and `quoteChar` on input, `delimiter` and `newline` on output. The core never reads this, so it is the one object in the config where unknown keys are allowed."
    },
    "__schema18": {},
    "__schema19": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/InputField"
      },
      "description": "The source columns to read and show read-only."
    },
    "InputField": {
      "title": "Input field",
      "description": "One source column, read from the input file and shown read-only.",
      "type": "object",
      "properties": {
        "type": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "text",
              "description": "A string."
            },
            {
              "type": "string",
              "const": "integer",
              "description": "A whole number."
            },
            {
              "type": "string",
              "const": "number",
              "description": "A number, whole or fractional."
            },
            {
              "type": "string",
              "const": "boolean",
              "description": "true / false."
            },
            {
              "type": "string",
              "const": "date",
              "description": "A date; ISO-8601 on the wire."
            },
            {
              "type": "string",
              "const": "enum",
              "description": "One of a fixed set of `choices`."
            },
            {
              "type": "string",
              "const": "object",
              "description": "A record with named `fields`."
            },
            {
              "type": "string",
              "const": "array",
              "description": "A list of `items`."
            },
            {
              "type": "string",
              "const": "map",
              "description": "A dictionary of `keys` → `values`."
            }
          ]
        },
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        }
      },
      "required": ["type", "name"],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "text"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema20"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "integer"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema28"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "number"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema29"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "boolean"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema30"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "date"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema31"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "enum"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema32"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "object"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema39"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "array"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema66"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "map"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema67"
          }
        }
      ]
    },
    "__schema20": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "text",
          "description": "A string."
        },
        "minLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Shortest accepted value, in characters."
        },
        "maxLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Longest accepted value, in characters."
        },
        "pattern": {
          "type": "string",
          "description": "JavaScript regular expression the value must match. Compiled when the config loads, so a malformed pattern fails at load rather than at render."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema21": {
      "description": "The column name in the data file. Letters, digits, `_` and `-`, with optional inner spaces; `.` `[` `]` `*` and edge whitespace are excluded.",
      "$ref": "#/$defs/__schema6"
    },
    "__schema22": {
      "$ref": "#/$defs/FieldDisplay"
    },
    "FieldDisplay": {
      "type": "object",
      "properties": {
        "title": {
          "$ref": "#/$defs/__schema23"
        },
        "description": {
          "$ref": "#/$defs/__schema24"
        },
        "help": {
          "$ref": "#/$defs/__schema25"
        },
        "titlePosition": {
          "$ref": "#/$defs/__schema26"
        },
        "textSize": {
          "$ref": "#/$defs/__schema27"
        }
      },
      "additionalProperties": false,
      "title": "Field display",
      "description": "Captions plus layout. Only fields take the layout keys — choices, cards and table columns take a plain text display."
    },
    "__schema23": {
      "type": "string",
      "description": "The caption shown in place of the machine name."
    },
    "__schema24": {
      "type": "string",
      "description": "Shown under the caption."
    },
    "__schema25": {
      "type": "string",
      "description": "Shown in the ⓘ popover beside the caption."
    },
    "__schema26": {
      "type": "string",
      "enum": ["left", "above"],
      "description": "Where the caption sits relative to the value. Defaults to `left`."
    },
    "__schema27": {
      "type": "string",
      "enum": ["sm", "md", "lg"],
      "description": "Size of the rendered value. Defaults to `md`."
    },
    "__schema28": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "integer",
          "description": "A whole number."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint — three slider clicks legitimately produce 0.30000000000000004."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema29": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "number",
          "description": "A number, whole or fractional."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema30": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "boolean",
          "description": "true / false."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema31": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "date",
          "description": "A date; ISO-8601 on the wire."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema32": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "enum",
          "description": "One of a fixed set of `choices`."
        },
        "choices": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/Choice"
          },
          "description": "The permitted values, in the order they are offered."
        }
      },
      "required": ["name", "type", "choices"],
      "additionalProperties": false
    },
    "Choice": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema33"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema34"
            }
          ]
        },
        "shortcut": {
          "$ref": "#/$defs/__schema35"
        },
        "selectedStyle": {
          "$ref": "#/$defs/__schema36"
        }
      },
      "required": ["name"],
      "additionalProperties": false,
      "title": "Choice",
      "description": "One option of an `enum`."
    },
    "__schema33": {
      "type": "string",
      "minLength": 1,
      "description": "The literal string written to the data file when this choice is selected."
    },
    "__schema34": {
      "$ref": "#/$defs/TextDisplay"
    },
    "TextDisplay": {
      "type": "object",
      "properties": {
        "title": {
          "$ref": "#/$defs/__schema23"
        },
        "description": {
          "$ref": "#/$defs/__schema24"
        },
        "help": {
          "$ref": "#/$defs/__schema25"
        }
      },
      "additionalProperties": false,
      "title": "Text display",
      "description": "Captions for one entity. A bare string is shorthand for `{ title }`, which is how most display blocks are written."
    },
    "__schema35": {
      "type": "string",
      "description": "Chord that picks this choice. Fires app-wide, not only while the field has focus, so it shares one namespace with every other shortcut in the config."
    },
    "__schema36": {
      "description": "Applied to the widget while this choice is the selected one.",
      "$ref": "#/$defs/Style"
    },
    "__schema37": {
      "type": "string",
      "enum": ["muted", "info", "success", "warning", "danger", "accent"],
      "description": "Semantic tone. Each maps to a contrast-audited token pair rather than a fixed colour, so both themes stay legible."
    },
    "__schema38": {
      "type": "string",
      "description": "Short explanation shown beside the styled value."
    },
    "Style": {
      "type": "object",
      "properties": {
        "tone": {
          "$ref": "#/$defs/__schema37"
        },
        "note": {
          "$ref": "#/$defs/__schema38"
        }
      },
      "additionalProperties": false,
      "title": "Style"
    },
    "__schema39": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "object",
          "description": "A record with named `fields`."
        },
        "fields": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/NestedField"
          },
          "description": "The object's own named members. Unlike a column name, these are JSON keys from the source payload, so colons, slashes and unicode are all allowed."
        },
        "table": {
          "$ref": "#/$defs/TableView"
        }
      },
      "required": ["name", "type", "fields"],
      "additionalProperties": false
    },
    "NestedField": {
      "title": "Nested field",
      "description": "A named member inside an `object`.",
      "type": "object",
      "properties": {
        "type": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "text",
              "description": "A string."
            },
            {
              "type": "string",
              "const": "integer",
              "description": "A whole number."
            },
            {
              "type": "string",
              "const": "number",
              "description": "A number, whole or fractional."
            },
            {
              "type": "string",
              "const": "boolean",
              "description": "true / false."
            },
            {
              "type": "string",
              "const": "date",
              "description": "A date; ISO-8601 on the wire."
            },
            {
              "type": "string",
              "const": "enum",
              "description": "One of a fixed set of `choices`."
            },
            {
              "type": "string",
              "const": "object",
              "description": "A record with named `fields`."
            },
            {
              "type": "string",
              "const": "array",
              "description": "A list of `items`."
            },
            {
              "type": "string",
              "const": "map",
              "description": "A dictionary of `keys` → `values`."
            }
          ]
        },
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        }
      },
      "required": ["type", "name"],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "text"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema40"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "integer"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema43"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "number"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema44"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "boolean"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema45"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "date"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema46"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "enum"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema47"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "object"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema48"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "array"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema55"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "map"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema65"
          }
        }
      ]
    },
    "__schema40": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "text",
          "description": "A string."
        },
        "minLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Shortest accepted value, in characters."
        },
        "maxLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Longest accepted value, in characters."
        },
        "pattern": {
          "type": "string",
          "description": "JavaScript regular expression the value must match. Compiled when the config loads, so a malformed pattern fails at load rather than at render."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema41": {
      "type": "string",
      "minLength": 1,
      "description": "The key this member has in the source payload. Deliberately unconstrained, unlike a column name — real payloads use colons, slashes and unicode."
    },
    "__schema42": {
      "$ref": "#/$defs/TextDisplay"
    },
    "__schema43": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "integer",
          "description": "A whole number."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint — three slider clicks legitimately produce 0.30000000000000004."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema44": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "number",
          "description": "A number, whole or fractional."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema45": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "boolean",
          "description": "true / false."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema46": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "date",
          "description": "A date; ISO-8601 on the wire."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema47": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "enum",
          "description": "One of a fixed set of `choices`."
        },
        "choices": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/Choice"
          },
          "description": "The permitted values, in the order they are offered."
        }
      },
      "required": ["name", "type", "choices"],
      "additionalProperties": false
    },
    "__schema48": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "object",
          "description": "A record with named `fields`."
        },
        "fields": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/NestedField"
          },
          "description": "The object's own named members. Unlike a column name, these are JSON keys from the source payload, so colons, slashes and unicode are all allowed."
        },
        "table": {
          "$ref": "#/$defs/TableView"
        }
      },
      "required": ["name", "type", "fields"],
      "additionalProperties": false
    },
    "TableView": {
      "type": "object",
      "properties": {
        "columns": {
          "$ref": "#/$defs/__schema49"
        }
      },
      "required": ["columns"],
      "additionalProperties": false,
      "title": "Table view",
      "description": "How an object's fields group into displayed columns. Lives on the `object` type beside the `fields` its columns name, so it nests to any depth and covers `map<_, object>` as well as arrays."
    },
    "__schema49": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/TableColumn"
      },
      "description": "The displayed columns, left to right."
    },
    "TableColumn": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema50"
        },
        "use": {
          "$ref": "#/$defs/__schema51"
        },
        "layout": {
          "$ref": "#/$defs/__schema53"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema54"
            }
          ]
        }
      },
      "required": ["name", "use"],
      "additionalProperties": false,
      "title": "Table column",
      "description": "One displayed column, combining one or more of the object's own fields."
    },
    "__schema50": {
      "type": "string",
      "minLength": 1,
      "description": "The column heading."
    },
    "__schema51": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema52"
      },
      "description": "Object field names combined into this one column."
    },
    "__schema52": {
      "type": "string",
      "minLength": 1
    },
    "__schema53": {
      "type": "string",
      "enum": ["chips", "stack", "inline"],
      "description": "How multiple values share the cell. Defaults to `chips`."
    },
    "__schema54": {
      "$ref": "#/$defs/TextDisplay"
    },
    "__schema55": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "array",
          "description": "A list of `items`."
        },
        "items": {
          "description": "The type of every element. An `array` of `enum` is how a multi-select is declared.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "items"],
      "additionalProperties": false
    },
    "__schema56": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "text",
          "description": "A string."
        },
        "minLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Shortest accepted value, in characters."
        },
        "maxLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Longest accepted value, in characters."
        },
        "pattern": {
          "type": "string",
          "description": "JavaScript regular expression the value must match. Compiled when the config loads, so a malformed pattern fails at load rather than at render."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    },
    "__schema57": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "integer",
          "description": "A whole number."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint — three slider clicks legitimately produce 0.30000000000000004."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    },
    "__schema58": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "number",
          "description": "A number, whole or fractional."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    },
    "__schema59": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "boolean",
          "description": "true / false."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    },
    "__schema60": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "date",
          "description": "A date; ISO-8601 on the wire."
        }
      },
      "required": ["type"],
      "additionalProperties": false
    },
    "__schema61": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "enum",
          "description": "One of a fixed set of `choices`."
        },
        "choices": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/Choice"
          },
          "description": "The permitted values, in the order they are offered."
        }
      },
      "required": ["type", "choices"],
      "additionalProperties": false
    },
    "__schema62": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "object",
          "description": "A record with named `fields`."
        },
        "fields": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/NestedField"
          },
          "description": "The object's own named members. Unlike a column name, these are JSON keys from the source payload, so colons, slashes and unicode are all allowed."
        },
        "table": {
          "$ref": "#/$defs/TableView"
        }
      },
      "required": ["type", "fields"],
      "additionalProperties": false
    },
    "__schema63": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "array",
          "description": "A list of `items`."
        },
        "items": {
          "description": "The type of every element. An `array` of `enum` is how a multi-select is declared.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["type", "items"],
      "additionalProperties": false
    },
    "__schema64": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "map",
          "description": "A dictionary of `keys` → `values`."
        },
        "keys": {
          "description": "The type of each key. Defaults to text when omitted.",
          "$ref": "#/$defs/ValueType"
        },
        "values": {
          "description": "The type of each value.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["type", "values"],
      "additionalProperties": false
    },
    "ValueType": {
      "title": "Value type",
      "type": "object",
      "properties": {
        "type": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "text",
              "description": "A string."
            },
            {
              "type": "string",
              "const": "integer",
              "description": "A whole number."
            },
            {
              "type": "string",
              "const": "number",
              "description": "A number, whole or fractional."
            },
            {
              "type": "string",
              "const": "boolean",
              "description": "true / false."
            },
            {
              "type": "string",
              "const": "date",
              "description": "A date; ISO-8601 on the wire."
            },
            {
              "type": "string",
              "const": "enum",
              "description": "One of a fixed set of `choices`."
            },
            {
              "type": "string",
              "const": "object",
              "description": "A record with named `fields`."
            },
            {
              "type": "string",
              "const": "array",
              "description": "A list of `items`."
            },
            {
              "type": "string",
              "const": "map",
              "description": "A dictionary of `keys` → `values`."
            }
          ]
        }
      },
      "required": ["type"],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "text"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema56"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "integer"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema57"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "number"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema58"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "boolean"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema59"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "date"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema60"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "enum"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema61"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "object"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema62"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "array"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema63"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "map"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema64"
          }
        }
      ]
    },
    "__schema65": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema41"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema42"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "map",
          "description": "A dictionary of `keys` → `values`."
        },
        "keys": {
          "description": "The type of each key. Defaults to text when omitted.",
          "$ref": "#/$defs/ValueType"
        },
        "values": {
          "description": "The type of each value.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "values"],
      "additionalProperties": false
    },
    "__schema66": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "array",
          "description": "A list of `items`."
        },
        "items": {
          "description": "The type of every element. An `array` of `enum` is how a multi-select is declared.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "items"],
      "additionalProperties": false
    },
    "__schema67": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema22"
            }
          ]
        },
        "type": {
          "type": "string",
          "const": "map",
          "description": "A dictionary of `keys` → `values`."
        },
        "keys": {
          "description": "The type of each key. Defaults to text when omitted.",
          "$ref": "#/$defs/ValueType"
        },
        "values": {
          "description": "The type of each value.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "values"],
      "additionalProperties": false
    },
    "__schema68": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/DisplayRule"
      },
      "description": "Purely visual rules over displayed input values. A rule can never change what is exported."
    },
    "DisplayRule": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema69"
        },
        "when": {
          "$ref": "#/$defs/__schema70"
        },
        "appliesTo": {
          "$ref": "#/$defs/__schema98"
        },
        "appliesToCards": {
          "$ref": "#/$defs/__schema99"
        },
        "forEach": {
          "$ref": "#/$defs/__schema100"
        },
        "style": {
          "$ref": "#/$defs/__schema101"
        }
      },
      "required": ["name", "when", "style"],
      "additionalProperties": false,
      "title": "Display rule",
      "description": "A purely visual rule over displayed input values. Rules never affect what is exported — that guarantee is structural, since the module evaluating them is never imported on the export path."
    },
    "__schema69": {
      "description": "Identifies the rule; shown in diagnostics and used as a stable render key.",
      "$ref": "#/$defs/__schema6"
    },
    "__schema70": {
      "description": "The condition that fires this rule.",
      "$ref": "#/$defs/Condition"
    },
    "__schema71": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "eq",
          "description": "True when the values are equal."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "description": "The literal to compare against. Give this or `otherField`, not both.",
          "$ref": "#/$defs/__schema76"
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema72": {
      "description": "The input field whose value is tested.",
      "$ref": "#/$defs/__schema6"
    },
    "__schema73": {
      "type": "string"
    },
    "__schema74": {
      "type": "number"
    },
    "__schema75": {
      "type": "boolean"
    },
    "__schema76": {
      "anyOf": [
        {
          "$ref": "#/$defs/__schema73"
        },
        {
          "$ref": "#/$defs/__schema74"
        },
        {
          "$ref": "#/$defs/__schema75"
        }
      ]
    },
    "__schema77": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "ne",
          "description": "True when the values differ."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "description": "The literal to compare against. Give this or `otherField`, not both.",
          "$ref": "#/$defs/__schema76"
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema78": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "gt",
          "description": "True when the field is greater than the comparand."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "type": "number",
          "description": "The literal to compare against. Give this or `otherField`, not both."
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema79": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "gte",
          "description": "True when the field is greater than or equal to the comparand."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "type": "number",
          "description": "The literal to compare against. Give this or `otherField`, not both."
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema80": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "lt",
          "description": "True when the field is less than the comparand."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "type": "number",
          "description": "The literal to compare against. Give this or `otherField`, not both."
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema81": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "lte",
          "description": "True when the field is less than or equal to the comparand."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "type": "number",
          "description": "The literal to compare against. Give this or `otherField`, not both."
        },
        "otherField": {
          "description": "Another input field to compare against, for rules that relate two columns. Give this or `value`, not both.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema82": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "in",
          "description": "True when the field matches one of the listed values."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/__schema76"
          },
          "description": "The values to match against."
        }
      },
      "required": ["op", "field", "value"],
      "additionalProperties": false
    },
    "__schema83": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "notIn",
          "description": "True when the field matches none of the listed values."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "value": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/__schema76"
          },
          "description": "The values to match against."
        }
      },
      "required": ["op", "field", "value"],
      "additionalProperties": false
    },
    "__schema84": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "matches",
          "description": "True when the field matches the regular expression."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "pattern": {
          "type": "string",
          "minLength": 1,
          "description": "JavaScript regular expression, tested against the value's string form. Compiled when the config loads."
        }
      },
      "required": ["op", "field", "pattern"],
      "additionalProperties": false
    },
    "__schema85": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "empty",
          "description": "True when the field is null, an empty string, or an empty list."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema86": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "notEmpty",
          "description": "True when the field holds anything at all."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        }
      },
      "required": ["op", "field"],
      "additionalProperties": false
    },
    "__schema87": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "exceedsFactor",
          "description": "True when the field is more than `factor` times the comparand — a difference in magnitude rather than merely being larger. Defined only over non-negative magnitudes: a negative value, or a comparand of zero or less, does not fire."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "otherField": {
          "description": "The input field this one is measured against. Required — this operator has no literal form.",
          "$ref": "#/$defs/__schema6"
        },
        "factor": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "The multiple to scale the comparand by. `3` reads as 'three times'. A factor of 1 is legal and degenerates into a plain `gt` / `lt`."
        }
      },
      "required": ["op", "field", "otherField", "factor"],
      "additionalProperties": false
    },
    "__schema88": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "fallsBelowFactor",
          "description": "True when the field is less than the comparand divided by `factor`. Defined only over non-negative magnitudes."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "otherField": {
          "description": "The input field this one is measured against. Required — this operator has no literal form.",
          "$ref": "#/$defs/__schema6"
        },
        "factor": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "The multiple to scale the comparand by. `3` reads as 'three times'. A factor of 1 is legal and degenerates into a plain `gt` / `lt`."
        }
      },
      "required": ["op", "field", "otherField", "factor"],
      "additionalProperties": false
    },
    "__schema89": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "sameDomain",
          "description": "True when both values carry an email domain and the domains match, compared without regard to case. A value with no `@` has no domain, so it never matches."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "otherField": {
          "description": "The input field this one is measured against. Required — this operator has no literal form.",
          "$ref": "#/$defs/__schema6"
        },
        "ignore": {
          "description": "Domains that never count as shared, compared without regard to case — the large free providers, typically. Two accounts both at `gmail.com` have nothing in common worth colouring.",
          "$ref": "#/$defs/__schema90"
        }
      },
      "required": ["op", "field", "otherField"],
      "additionalProperties": false
    },
    "__schema90": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "__schema91": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "sameLocalPart",
          "description": "True when both values share a local part — the text before the `@`, or the whole value when there is no `@` — compared without regard to case. That fallback is what lets a bare username be tested against a sender address."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "otherField": {
          "description": "The input field this one is measured against. Required — this operator has no literal form.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["op", "field", "otherField"],
      "additionalProperties": false
    },
    "__schema92": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "sharesPrefix",
          "description": "True when both values begin with the same `length` characters, compared without regard to case. For spotting identifiers minted together — a run of handles all starting `Bvd` — where what matters is that two values resemble each other, not what either looks like alone. A value shorter than `length` never matches."
        },
        "field": {
          "$ref": "#/$defs/__schema72"
        },
        "otherField": {
          "description": "The input field this one is measured against. Required — this operator has no literal form.",
          "$ref": "#/$defs/__schema6"
        },
        "length": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "description": "How many leading characters must agree. Low values match by coincidence — 3 or 4 is usually where a shared prefix stops being an accident."
        }
      },
      "required": ["op", "field", "otherField", "length"],
      "additionalProperties": false
    },
    "__schema93": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "allOf",
          "description": "True when every listed condition holds. Conditions may nest."
        },
        "conditions": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/__schema94"
          },
          "description": "Every one of these must hold."
        }
      },
      "required": ["op", "conditions"],
      "additionalProperties": false
    },
    "__schema94": {
      "$ref": "#/$defs/Condition"
    },
    "Condition": {
      "title": "Condition",
      "description": "A rule's trigger, evaluated over one record's input values. Never throws: a condition pointed at a missing or wrongly-typed value simply does not fire.",
      "type": "object",
      "properties": {
        "op": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "eq",
              "description": "True when the values are equal."
            },
            {
              "type": "string",
              "const": "ne",
              "description": "True when the values differ."
            },
            {
              "type": "string",
              "const": "gt",
              "description": "True when the field is greater than the comparand."
            },
            {
              "type": "string",
              "const": "gte",
              "description": "True when the field is greater than or equal to the comparand."
            },
            {
              "type": "string",
              "const": "lt",
              "description": "True when the field is less than the comparand."
            },
            {
              "type": "string",
              "const": "lte",
              "description": "True when the field is less than or equal to the comparand."
            },
            {
              "type": "string",
              "const": "in",
              "description": "True when the field matches one of the listed values."
            },
            {
              "type": "string",
              "const": "notIn",
              "description": "True when the field matches none of the listed values."
            },
            {
              "type": "string",
              "const": "matches",
              "description": "True when the field matches the regular expression."
            },
            {
              "type": "string",
              "const": "empty",
              "description": "True when the field is null, an empty string, or an empty list."
            },
            {
              "type": "string",
              "const": "notEmpty",
              "description": "True when the field holds anything at all."
            },
            {
              "type": "string",
              "const": "exceedsFactor",
              "description": "True when the field is more than `factor` times the comparand — a difference in magnitude rather than merely being larger. Defined only over non-negative magnitudes: a negative value, or a comparand of zero or less, does not fire."
            },
            {
              "type": "string",
              "const": "fallsBelowFactor",
              "description": "True when the field is less than the comparand divided by `factor`. Defined only over non-negative magnitudes."
            },
            {
              "type": "string",
              "const": "sameDomain",
              "description": "True when both values carry an email domain and the domains match, compared without regard to case. A value with no `@` has no domain, so it never matches."
            },
            {
              "type": "string",
              "const": "sameLocalPart",
              "description": "True when both values share a local part — the text before the `@`, or the whole value when there is no `@` — compared without regard to case. That fallback is what lets a bare username be tested against a sender address."
            },
            {
              "type": "string",
              "const": "sharesPrefix",
              "description": "True when both values begin with the same `length` characters, compared without regard to case. For spotting identifiers minted together — a run of handles all starting `Bvd` — where what matters is that two values resemble each other, not what either looks like alone. A value shorter than `length` never matches."
            },
            {
              "type": "string",
              "const": "allOf",
              "description": "True when every listed condition holds. Conditions may nest."
            },
            {
              "type": "string",
              "const": "anyOf",
              "description": "True when at least one listed condition holds. Conditions may nest."
            },
            {
              "type": "string",
              "const": "not",
              "description": "True when the nested condition does not hold."
            }
          ]
        }
      },
      "required": ["op"],
      "allOf": [
        {
          "if": {
            "properties": {
              "op": {
                "const": "eq"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema71"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "ne"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema77"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "gt"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema78"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "gte"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema79"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "lt"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema80"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "lte"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema81"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "in"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema82"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "notIn"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema83"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "matches"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema84"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "empty"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema85"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "notEmpty"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema86"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "exceedsFactor"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema87"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "fallsBelowFactor"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema88"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "sameDomain"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema89"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "sameLocalPart"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema91"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "sharesPrefix"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema92"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "allOf"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema93"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "anyOf"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema95"
          }
        },
        {
          "if": {
            "properties": {
              "op": {
                "const": "not"
              }
            },
            "required": ["op"]
          },
          "then": {
            "$ref": "#/$defs/__schema97"
          }
        }
      ]
    },
    "__schema95": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "anyOf",
          "description": "True when at least one listed condition holds. Conditions may nest."
        },
        "conditions": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/__schema96"
          },
          "description": "At least one of these must hold."
        }
      },
      "required": ["op", "conditions"],
      "additionalProperties": false
    },
    "__schema96": {
      "$ref": "#/$defs/Condition"
    },
    "__schema97": {
      "type": "object",
      "properties": {
        "op": {
          "type": "string",
          "const": "not",
          "description": "True when the nested condition does not hold."
        },
        "condition": {
          "description": "The condition to negate.",
          "$ref": "#/$defs/Condition"
        }
      },
      "required": ["op", "condition"],
      "additionalProperties": false
    },
    "__schema98": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema6"
      },
      "description": "Fields to style when the rule fires. Defaults to the field the condition tests, which is what you want for a single-field rule and never what you want when comparing two. The default is skipped entirely when `appliesToCards` is given."
    },
    "__schema99": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema6"
      },
      "description": "Input cards to annotate when the rule fires. A card-level note states once what would otherwise repeat on every field in the group. This is a separate key from `appliesTo` because card names and field names live in separate namespaces — a card may legitimately share a name with a field, and one list could not tell them apart."
    },
    "__schema100": {
      "description": "Evaluate this rule once per element of the named input field, which must be an `array`, and style the elements it holds for. Inside the loop, an element of an `array` of `object` is addressed by its own field names, which shadow a record column of the same name; an element of a list of scalars is addressed by the list's own name. Either way every other record column stays readable, so `otherField` compares an element against the record around it. Cannot be combined with `appliesTo` or `appliesToCards` — the target is always the matching element.",
      "$ref": "#/$defs/__schema6"
    },
    "__schema101": {
      "description": "How the matched fields are styled.",
      "$ref": "#/$defs/Style"
    },
    "__schema102": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Card"
      },
      "description": "Layout. Omit for one implicit card holding every field, one per row."
    },
    "Card": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema103"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema104"
            }
          ]
        },
        "scope": {
          "$ref": "#/$defs/__schema105"
        },
        "rows": {
          "$ref": "#/$defs/__schema106"
        }
      },
      "required": ["name", "rows"],
      "additionalProperties": false,
      "title": "Card",
      "description": "A card grouping fields under a heading. The same shape drives read-only input cards and interactive output cards. Omit `cards` entirely for one implicit card holding every field, one per row."
    },
    "__schema103": {
      "description": "Identifies the card; must be unique per side.",
      "$ref": "#/$defs/__schema6"
    },
    "__schema104": {
      "$ref": "#/$defs/TextDisplay"
    },
    "__schema105": {
      "type": "string",
      "enum": ["record", "session"],
      "description": "`session` cards render on the setup step rather than once per record. Defaults to `record`."
    },
    "__schema106": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/CardRow"
      },
      "description": "The card's rows, rendered top to bottom."
    },
    "CardRow": {
      "type": "object",
      "properties": {
        "perRow": {
          "$ref": "#/$defs/__schema107"
        },
        "use": {
          "$ref": "#/$defs/__schema108"
        }
      },
      "required": ["use"],
      "additionalProperties": false,
      "title": "Card row"
    },
    "__schema107": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "description": "Columns to fit before wrapping. Defaults to the number of fields in the row."
    },
    "__schema108": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/__schema6"
      },
      "description": "Field names to render in this row, in order."
    },
    "__schema109": {
      "type": "object",
      "properties": {
        "adapterId": {
          "$ref": "#/$defs/__schema15"
        },
        "adapterConfig": {
          "$ref": "#/$defs/__schema17"
        },
        "fields": {
          "$ref": "#/$defs/__schema110"
        },
        "cards": {
          "$ref": "#/$defs/__schema128"
        }
      },
      "required": ["fields"],
      "additionalProperties": false,
      "description": "What the labeler captures, or the app fills in."
    },
    "__schema110": {
      "minItems": 1,
      "type": "array",
      "items": {
        "$ref": "#/$defs/OutputField"
      },
      "description": "The columns written to the output file."
    },
    "OutputField": {
      "title": "Output field",
      "description": "One column written to the output file — captured from the labeler, copied from the input, or derived by the app.",
      "type": "object",
      "properties": {
        "type": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "text",
              "description": "A string."
            },
            {
              "type": "string",
              "const": "integer",
              "description": "A whole number."
            },
            {
              "type": "string",
              "const": "number",
              "description": "A number, whole or fractional."
            },
            {
              "type": "string",
              "const": "boolean",
              "description": "true / false."
            },
            {
              "type": "string",
              "const": "date",
              "description": "A date; ISO-8601 on the wire."
            },
            {
              "type": "string",
              "const": "enum",
              "description": "One of a fixed set of `choices`."
            },
            {
              "type": "string",
              "const": "object",
              "description": "A record with named `fields`."
            },
            {
              "type": "string",
              "const": "array",
              "description": "A list of `items`."
            },
            {
              "type": "string",
              "const": "map",
              "description": "A dictionary of `keys` → `values`."
            }
          ]
        },
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        }
      },
      "required": ["type", "name"],
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "text"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema111"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "integer"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema120"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "number"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema121"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "boolean"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema122"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "date"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema123"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "enum"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema124"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "object"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema125"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "array"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema126"
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "map"
              }
            },
            "required": ["type"]
          },
          "then": {
            "$ref": "#/$defs/__schema127"
          }
        }
      ]
    },
    "__schema111": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["text", "textarea"],
          "description": "How the field renders. Defaults to `text`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "text",
          "description": "A string."
        },
        "minLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Shortest accepted value, in characters."
        },
        "maxLength": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Longest accepted value, in characters."
        },
        "pattern": {
          "type": "string",
          "description": "JavaScript regular expression the value must match. Compiled when the config loads, so a malformed pattern fails at load rather than at render."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema112": {
      "$ref": "#/$defs/FieldDisplay"
    },
    "__schema113": {
      "description": "Where this field's value comes from. Defaults to `{ \"kind\": \"user\" }`.",
      "$ref": "#/$defs/Fill"
    },
    "__schema114": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "user",
          "description": "The labeler answers this field on every record."
        }
      },
      "required": ["kind"],
      "additionalProperties": false
    },
    "__schema115": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "copy",
          "description": "Carried over from an input column. Renders no widget."
        },
        "from": {
          "description": "Input field to copy from. Defaults to this field's own name; naming one explicitly is how a copied column gets renamed on the way out. Its type must match this field's type.",
          "$ref": "#/$defs/__schema6"
        }
      },
      "required": ["kind"],
      "additionalProperties": false
    },
    "__schema116": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "session",
          "description": "The labeler answers this once, on the setup step, and the answer is written to every exported row."
        }
      },
      "required": ["kind"],
      "additionalProperties": false
    },
    "__schema117": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "const": "timestamp",
          "description": "Stamped by the app when the record becomes complete, and again on every later edit. Renders no widget."
        }
      },
      "required": ["kind"],
      "additionalProperties": false
    },
    "Fill": {
      "title": "Fill",
      "description": "Where an output field's value comes from — orthogonal to how it renders. Only `user` and `session` fields show a widget.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "Selects which shape the rest of this object takes. Every other key is validated against the variant named here.",
          "anyOf": [
            {
              "type": "string",
              "const": "user",
              "description": "The labeler answers this field on every record."
            },
            {
              "type": "string",
              "const": "copy",
              "description": "Carried over from an input column. Renders no widget."
            },
            {
              "type": "string",
              "const": "session",
              "description": "The labeler answers this once, on the setup step, and the answer is written to every exported row."
            },
            {
              "type": "string",
              "const": "timestamp",
              "description": "Stamped by the app when the record becomes complete, and again on every later edit. Renders no widget."
            }
          ]
        }
      },
      "required": ["kind"],
      "allOf": [
        {
          "if": {
            "properties": {
              "kind": {
                "const": "user"
              }
            },
            "required": ["kind"]
          },
          "then": {
            "$ref": "#/$defs/__schema114"
          }
        },
        {
          "if": {
            "properties": {
              "kind": {
                "const": "copy"
              }
            },
            "required": ["kind"]
          },
          "then": {
            "$ref": "#/$defs/__schema115"
          }
        },
        {
          "if": {
            "properties": {
              "kind": {
                "const": "session"
              }
            },
            "required": ["kind"]
          },
          "then": {
            "$ref": "#/$defs/__schema116"
          }
        },
        {
          "if": {
            "properties": {
              "kind": {
                "const": "timestamp"
              }
            },
            "required": ["kind"]
          },
          "then": {
            "$ref": "#/$defs/__schema117"
          }
        }
      ]
    },
    "__schema118": {
      "type": "boolean",
      "description": "Whether a record counts as complete without this field. Defaults by fill: something a person is asked for is required, something the app derives is not."
    },
    "__schema119": {
      "type": "string",
      "pattern": "^(?:(?:mod|ctrl|control|alt|opt|option|shift|meta|cmd|command)\\+)*(?:[A-Za-z0-9]|space|enter|escape|tab|left|right|up|down|home|end|backspace|delete|esc|spacebar|return|arrowleft|arrowright|arrowup|arrowdown|del)$",
      "description": "Chord that moves focus to this field's widget, e.g. `\"mod+1\"`. Unique across the whole config, and not one of the chords the app or OS reserves."
    },
    "__schema120": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["number", "slider"],
          "description": "How the field renders. Defaults to `number`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "integer",
          "description": "A whole number."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint — three slider clicks legitimately produce 0.30000000000000004."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema121": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["number", "slider"],
          "description": "How the field renders. Defaults to `number`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "number",
          "description": "A number, whole or fractional."
        },
        "min": {
          "type": "number",
          "description": "Smallest accepted value, inclusive."
        },
        "max": {
          "type": "number",
          "description": "Largest accepted value, inclusive."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Increment for the number and slider widgets. A hint only, never a validity constraint."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema122": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["checkbox"],
          "description": "How the field renders. Defaults to `checkbox`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "boolean",
          "description": "true / false."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema123": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["date"],
          "description": "How the field renders. Defaults to `date`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "date",
          "description": "A date; ISO-8601 on the wire."
        }
      },
      "required": ["name", "type"],
      "additionalProperties": false
    },
    "__schema124": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["select", "radio"],
          "description": "How the field renders. Defaults to `select`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "enum",
          "description": "One of a fixed set of `choices`."
        },
        "choices": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/Choice"
          },
          "description": "The permitted values, in the order they are offered."
        }
      },
      "required": ["name", "type", "choices"],
      "additionalProperties": false
    },
    "__schema125": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "type": {
          "type": "string",
          "const": "object",
          "description": "A record with named `fields`."
        },
        "fields": {
          "minItems": 1,
          "type": "array",
          "items": {
            "$ref": "#/$defs/NestedField"
          },
          "description": "The object's own named members. Unlike a column name, these are JSON keys from the source payload, so colons, slashes and unicode are all allowed."
        },
        "table": {
          "$ref": "#/$defs/TableView"
        }
      },
      "required": ["name", "type", "fields"],
      "additionalProperties": false
    },
    "__schema126": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "widget": {
          "type": "string",
          "enum": ["checkboxes"],
          "description": "How the field renders. Defaults to `checkboxes`. Only a field a person fills may name a widget."
        },
        "type": {
          "type": "string",
          "const": "array",
          "description": "A list of `items`."
        },
        "items": {
          "description": "The type of every element. An `array` of `enum` is how a multi-select is declared.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "items"],
      "additionalProperties": false
    },
    "__schema127": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/__schema21"
        },
        "display": {
          "anyOf": [
            {
              "type": "string",
              "description": "Shorthand for `{ \"title\": … }`, which is how most display blocks are written."
            },
            {
              "$ref": "#/$defs/__schema112"
            }
          ]
        },
        "fill": {
          "$ref": "#/$defs/__schema113"
        },
        "required": {
          "$ref": "#/$defs/__schema118"
        },
        "shortcut": {
          "$ref": "#/$defs/__schema119"
        },
        "type": {
          "type": "string",
          "const": "map",
          "description": "A dictionary of `keys` → `values`."
        },
        "keys": {
          "description": "The type of each key. Defaults to text when omitted.",
          "$ref": "#/$defs/ValueType"
        },
        "values": {
          "description": "The type of each value.",
          "$ref": "#/$defs/ValueType"
        }
      },
      "required": ["name", "type", "values"],
      "additionalProperties": false
    },
    "__schema128": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Card"
      },
      "description": "Layout. Omit for one implicit card holding every field, one per row."
    }
  }
}
